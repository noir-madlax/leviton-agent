# 销售趋势API规范

## API设计原则

### 1. 统一请求格式
- **方法**: POST
- **Content-Type**: application/json  
- **请求体**: 统一使用JSON格式，包含project_id和filters
- **响应**: 统一返回JSON，包含数据、元数据和统计信息

### 2. 标准筛选支持
所有API必须支持项目标准筛选条件：
- `categories`: 产品类别筛选
- `brands`: 品牌筛选  
- `segments`: 产品段筛选
- `extend_fields`: 扩展字段筛选

### 3. 业务特定参数
销售趋势API额外支持：
- `date_range`: 时间范围筛选
- `asin`: 特定产品ASIN（可选）
- `metric_type`: 指标类型（sales/price/revenue）
- `aggregation`: 聚合粒度（daily/weekly/monthly）

## 前端访问规范

### Chart数据请求模式
前端访问chart数据时必须提供：
1. **project_id** - 项目标识符
2. **filters** - 筛选条件对象（可选）
3. **业务参数** - 图表特定的参数（如时间范围）

### 标准调用方式
```typescript
// 使用统一的API调用函数
const result = await callDashboardAPI('trending/sales-trend', {
  project_id: projectId,
  filters: {
    categories: selectedCategories,
    brands: selectedBrands,
    segments: selectedSegments,
    extend_fields: extendFields
  },
  // 业务参数
  date_range: { start_date: startDate, end_date: endDate },
  asin: targetAsin,
  metric_type: metricType
});
```

## 后端实现规范

### 1. 模型定义
- **请求模型**: 继承或包含project_id和filters字段
- **响应模型**: 包含数据、元数据、统计信息
- **使用Pydantic**: 确保类型安全和自动文档生成

### 2. 服务类设计
- **继承BaseDashboardService**: 自动获得ASIN过滤和标准筛选
- **实现get_data()方法**: 返回格式化的数据结构
- **支持Mock数据**: 便于开发测试

### 3. API路由实现
- **使用装饰器**: `@log_request_response`记录请求日志
- **统一错误处理**: HTTPException格式化错误响应
- **参数验证**: 自动验证请求参数格式

## 数据库访问规范

### 1. 项目ASIN过滤
- 所有查询必须应用项目ASIN列表过滤
- 防止跨项目数据泄露
- 使用BaseDashboardService的`_apply_asin_filter()`

### 2. 标准筛选应用
- 按顺序应用：ASIN -> Category -> Brand -> Segment -> ExtendFields
- 使用`_apply_combined_filters()`方法

### 3. 查询优化
- 建立适当的数据库索引
- 考虑数据缓存策略
- 大数据量时使用分页

## 响应格式规范

### 成功响应结构
```json
{
  "data": "核心业务数据",
  "metadata": "请求元数据",
  "statistics": "汇总统计信息",
  "filters_applied": "实际应用的筛选条件"
}
```

### 错误响应结构
```json
{
  "status_code": 400|404|500,
  "detail": "错误描述信息"
}
```

## 测试规范

### 1. 必备测试场景
- 基础请求测试
- 筛选条件测试
- 参数验证测试
- 错误处理测试

### 2. 测试工具
- 提供curl测试脚本
- 提供示例请求JSON
- 集成pytest测试用例

### 3. Mock数据要求
- 模拟真实业务场景
- 包含边界情况测试数据
- 便于前端开发调试

## 文档规范

### 1. API文档要求
- 完整的请求响应示例
- 字段类型和描述说明
- 业务逻辑处理流程
- 错误码和处理建议

### 2. 集成指南
- 前端调用示例
- 后端路由注册方法
- 数据库表结构建议
- 部署配置说明
description:
globs:
alwaysApply: false
---
