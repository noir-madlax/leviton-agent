You are consolidating aspect categories extracted from customer reviews.

**ASPECT TYPE:** {aspect_type} - {aspect_definition}
{product_context}

**TASK:** Merge two batches of {aspect_type} aspect categories by grouping similar ones together while maintaining specificity.

**INPUT TAXONOMIES:**

Taxonomy A:
{taxonomy_a}

Taxonomy B:
{taxonomy_b}

**CONSOLIDATION RULES:**
1. **Group similar categories** - Combine categories that represent the same or very similar concept based on their definitions
2. **Preserve specificity** - Maintain the specific nature of each category type rather than creating broader, more generic categories
3. **Create precise names** - Use clear, descriptive names (words separated by spaces) for consolidated categories; each category must represent a SINGULAR, SPECIFIC concept with NO OVERLAP; RENAME categories when needed to make them more distinguishing and specific than the originals
4. **Complete mapping** - Every A_* and B_* ID must appear exactly once in the "ids" arrays - no duplicates or missing IDs
5. **Maintain consistent granularity** - All consolidated categories must be at the same level of specificity
6. **Ensure mutual exclusivity** - Categories must have clear, non-overlapping boundaries with no ambiguity
7. **Write focused definitions** - Create a precise definition for each consolidated category that clearly describes the specific aspect type, ending with "e.g." and specific examples

**OUTPUT FORMAT:**
Return only valid JSON with this structure:
```json
{
  "Category Name 1": {
    "definition": "Precise description ending with examples, e.g. specific examples",
    "ids": ["A_0", "B_1"]
  },
  "Category Name 2": {
    "definition": "Another precise description, e.g. more examples", 
    "ids": ["A_1"]
  }
}
```

**CRITICAL:** Group only categories that represent essentially the same aspect type - do not force unrelated categories together. Return JSON. 